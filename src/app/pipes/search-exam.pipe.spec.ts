import { SearchExamPipe } from './search-exam.pipe';

describe('SearchExamPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchExamPipe();
    expect(pipe).toBeTruthy();
  });
});
